# db_scripts

```
mosip_audit/     # DDL · schema AUDIT · table app_audit_log
db_upgrade_scripts/   # versioned upgrades
```

Prod: PostgreSQL · service `ddl-auto=none`. Local/test: H2 · profile `local`.
